/**
 * Framework Hooks — Barrel export.
 *
 * Tout agent importe depuis '@framework/hooks'.
 */

export { useAgent } from './useAgent';
export { useAgentStorage } from './useAgentStorage';
export { useWebSocket } from './useWebSocket';
